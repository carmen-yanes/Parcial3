<h3>{{viewTitle}}</h3>

<form action="/employee" method="POST" autocomplete="off">
<input type="hidden" name="_id" value="{{employee._id}}">
    <div class="form-group">
        <label>cita</label>
        <input type="text" class="form-control" name="cita" placeholder="cita" value="{{employee.cita}}">
        <div class="text-danger">
            {{employee.citaError}}
        </div>

    <div class="form-group">
        <label>libro</label>
        <input type="text" class="form-control" name="libro" placeholder="libro" value="{{employee.libro}}">
    </div>
    <div class="form-row">
        <div class="form-group col-md-6">
            <label>versiculo</label>
        <input type="text" class="form-control" name="versiculo" placeholder="versiculo" value="{{employee.versiculo}}">
        </div>
            <div class="form-group col-md-6">
            <label>cap</label>
        <input type="text" class="form-control" name="cap" placeholder="cap" value="{{employee.cap}}">
    </div>

                <label>comentario</label>
        <input type="text" class="form-control" name="comentario" placeholder="comentario" value="{{employee.comentario}}">
    </div>
        
<div class="form-group">
    <button type="submit"class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
    <a class="btn btn-secondary" href="/employee/list"> <i class="fa fa-list-alt"></i>View All</a>
</div>
    

</form>